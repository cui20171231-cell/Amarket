# A-market 总体架构

## 1. 文档定位

本文记录 `D:\Amarket` 当前仓库中已经存在的系统结构，供后续任务快速了解项目边界。
它不代表本机服务一定正在运行；实际运行状态必须另行检查。

## 2. 系统目标

A-market 是一套独立的 A 股行情采集与分析基础设施。当前主线是从同花顺金融接口获取全 A
快照、涨跌停池、板块指数、交易日历和日线数据，保存到本地 ClickHouse，并按固定采集编号
生成个股、全市场和板块派生结果。

系统同时提供受限制的本地查询入口，并预留飞书消息入口。查询入口只允许读取；飞书监听器目前
只接收和识别 `/A` 指令，尚未把指令接入行情查询或回复流程。

## 3. 总体数据流

```text
同花顺金融接口
        │
        ├─ 全 A 快照、涨停池、跌停池、炸板池
        ├─ 板块目录、成员和板块指数
        ├─ 交易日历
        └─ 日线与复权事件
        │
        ▼
Python 采集、派生与聚合程序（app/hithink）
        │
        ├─ 原始事实和任务状态
        ├─ 个股分钟派生
        ├─ 全市场状态
        ├─ 概念、行业、风格状态
        ├─ 日线及前复权结果
        └─ 19节点市场状态聚合包及就绪通知
        │
        ▼
ClickHouse：market 数据库
        │
        ├─ 命令行状态查询
        ├─ 本地 AI 只读查询入口
        └─ 未来的消息查询和其他只读消费端

飞书长连接监听器
        └─ 当前仅接收并识别 /A 指令，未接入上面的查询链路
```

## 4. 主要组成

| 位置 | 作用 | 当前边界 |
|---|---|---|
| `app/hithink/` | 行情接口、计划生成、采集、写库、派生、聚合、状态检查 | 项目主业务代码 |
| `sql/hithink_snapshot.sql` | `market` 数据库的建表及兼容升级语句 | 数据结构唯一事实来源 |
| `scripts/` | ClickHouse 检查、采集器、日线和板块映射任务脚本 | 脚本存在不等于任务已安装或运行 |
| `app/ai_gateway.py` | 本机 HTTP 方式的 AI 只读查询入口 | 监听 `127.0.0.1`，默认端口 `2091` |
| `app/clickhouse_plugin_server.py` | 通过标准输入输出提供相同的只读工具 | 供本地集成使用 |
| `app/clickhouse_readonly.py` | 限制查询类型、行数和超时 | 禁止写入类语句 |
| `feishu_amarket_bot/` | 飞书长连接消息监听 | 当前只记录并识别 `/A`，不执行查询、不发送回答 |
| `compose.yaml` | ClickHouse 容器定义 | 数据卷独立保存，端口仅绑定本机 |
| `docs/` | 业务口径和项目基线文档 | 文档不得替代实际代码与建表文件 |

## 5. 采集、派生与聚合主链

每个交易日由固定计划生成最多 254 个采集节点，统一编号为：

```text
YYYYMMDD001 ... YYYYMMDD254
```

编号由计划时间决定，不因成功、失败或缺失而移动。单节点的设计顺序是：

```text
生成或读取计划节点
        ↓
获取全 A、三类涨跌停池和板块指数事实
        ↓
写入原始事实
        ↓
生成个股分钟派生
        ↓
生成全市场状态和三类板块状态
        ↓
派生情绪、15分钟变化、板块迁移及核心候选
        ↓
19个固定节点进入独立聚合模块，生成本地数据包
        ↓
更新该节点各阶段状态
```

代码边界固定为三部分：采集负责外部事实落库，派生负责数据库状态与候选表，聚合负责19个
固定节点的数据包落盘。聚合实现位于 `app/hithink/aggregation.py`，不再放在派生模块中，
也不再向外部聊天服务发送就绪通知。

详细口径见：

- [全市场状态派生](docs/hithink_market_state_derivation.md)
- [板块状态派生](docs/hithink_sector_state_derivation.md)
- [V2.0范围及与V1.0差异](docs/V2.0_SCOPE.md)
- [当前运行基线与长期目标](docs/CURRENT_STATE_AND_LONG_TERM_GOAL.md)

## 6. 数据层

当前数据库是 ClickHouse，默认数据库名为 `market`。表按职责分为：

- 调度与状态：交易日历、分钟计划、日线同步状态、板块映射同步状态。
- 原始事实：全 A 快照、三类涨跌停池、板块指数、日线和复权事件。
- 映射事实：板块目录和带有效期的板块成员历史。
- 派生结果：个股分钟派生、市场状态、概念状态、行业状态、风格状态和前复权日线。

完整表清单见 [数据库草案](docs/DATABASE_DRAFT.md)。任何结构变更应先修改并复核
`sql/hithink_snapshot.sql`，不能只改本文。

## 7. 运行与边界

- 主程序使用 Python 3.11 或更高版本。
- ClickHouse 的容器定义使用 `clickhouse/clickhouse-server:26.3`。
- 主采集器支持开机启动和失败后重试。每天 08:50 先确认交易日；仅在交易日先校验并补齐完整 254 节点计划，再执行最晚到 09:10 的板块映射，然后执行盘中节点。16:00 日K使用同一进程内的独立执行通道，不被第254号持续重试阻塞，失败后每10分钟重试到次日08:30；16:00后重启会补跑未成功的当日日K，次日08:30前重启会检查前一日。周一同步复权事件。非交易日不执行这些采集。板块映射和日线不再使用独立计划任务。
- 密钥从本地配置读取，不应写入仓库、日志或文档。
- `secrets/`、`data/`、`logs/`、`docker-data/` 属于运行期位置，不是产品文档来源。
- 未经明确授权，不启动、停止、重启、重算或回填任何任务和数据。
- A-market 与其他项目保持边界；后续任务不得因相邻目录或旧聊天内容擅自修改外部项目。

## 8. 当前已知缺口

- 飞书入口尚未连接到查询和回复链路。
- 仓库没有面向普通业务调用方的公开写入接口。
- 本文只确认仓库结构，不确认外部同花顺权限、本机计划任务或服务的实时状态。
- 产品需求、2.0当前边界、1.0历史边界、数据库概览和本地接口分别记录在 `docs` 对应文件中。
