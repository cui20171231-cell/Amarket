# N100 服务器改动日志

## 2026-08-29 第一阶段

状态：已完成，未重启系统

### 改动前基线

- 处理器：Intel N100，4 核。
- 物理内存：15.75 GB。
- 已用内存：约 8.65 GB。
- 可用内存：约 7.09 GB。
- ClickHouse 容器：健康，约使用 886 MB。
- 英特尔使用情况上报、显卡控制面板和性能监控相关进程：约 720 MB。
- Windows 小组件及相关网页进程：约 130 MB。
- 跨设备继续及相关系统服务：约 100 MB。

### 改动前已有系统设置

- Windows 小组件任务栏开关 `TaskbarDa` 已为 0。
- Windows 跨设备继续策略 `EnableCdp` 已为 0。
- Windows Search 系统服务已为禁用状态。

### 待记录的实际操作

#### 14:15 Intel 使用情况改进计划卸载

- 执行结果：失败，Windows Installer 返回 1603。
- 明确原因：安装日志错误 1730，当前会话没有提升后的管理员权限。
- 实际影响：软件未被卸载，其他服务未受影响。
- 安装程序原始日志：`Intel-CIP-卸载.log`。

#### 14:17 当前用户级精简

- 已删除 Chrome 开机自动启动项。
- 已删除 Edge 开机自动启动项。
- 已停止 `Widgets.exe` 和 `WidgetService.exe`。
- 已停止 `CrossDeviceResume.exe`。
- 已停止当前用户的 `IntelGraphicsSoftware.exe` 和 `esrv.exe`。
- 验证结果：两个浏览器启动项已不存在，上述五类用户进程均未立即重启。

#### 14:18 系统服务停止和禁用

- 尝试范围：Intel Collector、Intel Telemetry、Intel System Usage Report、Energy Server、Intel Graphics Software、PresentMon、Intel Graphics Command Center。
- 执行结果：Windows 全部拒绝，原因为当前会话没有提升后的管理员权限。
- 实际影响：这七项服务的启动类型和状态均未改变。

#### 管理员脚本

- 已生成 `执行第一阶段-管理员.ps1`，只包含本计划明确列出的操作。
- 已生成 `请以管理员身份运行.cmd`，用于由用户确认 Windows 管理员提示。
- 管理员脚本不会重启系统。

#### 14:20 管理员确认状态

- 已启动管理员执行入口并请求 Windows 管理员确认。
- 未生成 `管理员执行结果.log`，说明提升后的脚本尚未开始运行；不将系统级项目记为完成。

### 管理员执行前已完成项目

- Chrome 和 Edge 开机自动启动项已删除。
- Windows 小组件用户进程已停止。
- Windows 跨设备继续用户进程已停止。
- 英特尔显卡控制面板用户进程已停止。
- 英特尔当前用户上报进程已停止。

### 管理员执行前的待完成项目（随后已完成）

- 卸载 Intel(R) Computing Improvement Program。
- 停止并禁用 Intel Collector、Telemetry、System Usage Report 和 Energy Server。
- 停止并禁用 Intel Graphics Software、PresentMon 和 Graphics Command Center 附加服务。

### 部分验证

- ClickHouse 容器仍为健康状态。
- Tailscale、MarketOS 隧道和 ChatGPT/Codex 进程仍在运行。
- Intel UHD Graphics 设备状态仍为正常。
- 用户级可选进程未立即重启。
- 当时仍有约 504 MB 英特尔系统级附加服务在运行，随后已由管理员脚本处理。

#### 14:22–14:23 管理员脚本正式执行

- Windows 管理员提升成功，脚本于 14:22:32 开始，14:23:44 结束。
- Intel(R) Computing Improvement Program 卸载返回码为 0，表示成功。
- `IntelCollectorService`、`IntelTelemetryAgent`、`SystemUsageReportSvc_QUEENCREEK`、`ESRV_SVC_QUEENCREEK` 已被卸载程序移除。
- `PresentMonSharedService` 已被卸载程序移除。
- `IntelGraphicsSoftwareService` 已停止并设为禁用。
- `igccservice` 已停止并设为禁用。
- 未请求、未执行 Windows 重启。
- 完整执行记录：`管理员执行结果.log`。
- 成功卸载记录：`Intel-CIP-管理员卸载.log`。

### 最终验收结果

- Intel(R) Computing Improvement Program 安装记录已不存在。
- 第一阶段目标范围内的可选进程全部不再运行。
- Chrome 和 Edge 开机自动启动项均不存在。
- Intel UHD Graphics 显卡设备状态为正常。
- ClickHouse 容器仍为健康状态。
- `market` 数据库仍有 19 张表。
- `hithink_snapshot_raw` 合并后有效行数仍为 1,585,170。
- `MarketOSStatus-TrayIcons` 托盘任务仍为运行中。
- `HithinkSnapshotCollector` 采集任务仍为运行中。
- `HithinkSectorMappingSync` 和 `HithinkDailyPipeline` 均为待命、上次成功。
- Tailscale、MarketOS 隧道和 ChatGPT/Codex 仍在运行。

### 最终结论

第一阶段已完成。本次只移除或禁用了英特尔使用情况上报、显卡附加监控、Windows 小组件与跨设备用户进程，以及 Chrome/Edge 开机项。数据库、行情数据、采集任务、远程访问和显卡核心驱动未受影响。
