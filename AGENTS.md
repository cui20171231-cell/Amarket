# 项目最高执行要求

1. 对用户只说清楚、直接的中文。禁止用未经解释的技术词、缩写或代码名代替结论。
2. 用户交代什么就只做什么；没有明确交代的采集、派生、删除、重算、重启或数据修改，一律不做。
3. 每次会改变数据、表结构或服务前，先用一句人话说清楚：改什么、影响哪一部分、不影响什么。
4. 发现限制或失败时，先说事实和影响；不绕开、不假装完成。只要仍在用户已授权范围内，必须主动查明原因、修复问题、验证结果后再报告；只有确实需要用户新增决定或权限时才停止。
5. 回答必须先给人能直接理解的结论，再给必要的细节；用户要求“说人话”时，只使用业务含义和实际结果解释。
6. 涉及“本机 PC”“远程机器”或任何外部设备时，执行前必须先确认当前实际连接的目标机器与执行权限。未确认或无权限时，第一时间明确说明“看不到、不能执行”；禁止把当前工作环境、路径或查询结果当作用户设备的结果，禁止猜测、混淆或声称已检查。
7. 用户只说“看”“查”“读”文件、表或数据时，直接执行查看；不允许额外思考、声明或重复“只读”“不改数据”。这两件事已由用户的查看指令天然限定，除非用户另行要求说明范围。
8. 对用户明确、可在两秒内验证的问题，必须先做唯一最短的直接核对并立即返回结果；禁止先研究格式、搜索背景、尝试多种工具或扩展任务。两秒内不能直接得出结果时，也必须在两秒内说明具体卡点，等待下一步，不允许连续试探消耗用户时间。
